library(forecasgt)
data = ts(mdeaths)
model = auto.arima(data)
forecast = forecast(model, h=36)
plot(forecast)
library(forecast)
data = ts(mdeaths)
model = auto.arima(data)
forecast = forecast(model, h=36)
plot(forecast)
